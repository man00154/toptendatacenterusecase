# secrets.yaml
local_llm_model_path: "/app/models/gemini-2.0-flash-lite"  # mounted inside container
faiss_index_path: "/app/faiss_index"                        # persisted FAISS index
huggingface_token: "YOUR_HUGGINGFACE_TOKEN"                # optional
